import styles from "./Footer.module.scss";

const Footer = () => {
  return (
    <section className={styles.wrapper} id="footer">
      <p>Created by @mask.bright</p>
    </section>
  );
};

export default Footer;
